import React from 'react';
import './User.css';

const User = ({user}) => {
    
    
    return (
        <div>
            <div>{user.name}</div>
            
        </div>
    );
};

export default User;